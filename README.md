# weather-widget
Micro frontend to display weather data

## Getting Started

This repo uses pnpm.

Install dependencies with `pnpm i`

then build with `build:all`

Start the Micro frontend with `pnpm dev:search` and the page that hosts it with `pnpm dev:shell`
